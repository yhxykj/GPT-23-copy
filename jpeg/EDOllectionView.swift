
import UIKit

class EDOllectionView: UIView {
    private var aymentTable: String!
private var ableScreenCell: Double = 0.0
private var calendarNewsTableView: UITableView!
private var areaTimers: String!

 var vipSmallParameter: [String: Any]!
 var performStopMax_mcLabel: UILabel!
 var feedbackNext: Double = 0.0


    override init(frame: CGRect) {
        super.init(frame: frame)
        aymentTable = String(cString: [98,101,97,116,105,110,103,0], encoding: .utf8)!
        self.ableScreenCell = 5203.0
        calendarNewsTableView = UITableView()
        self.areaTimers = String(cString: [114,101,118,97,108,105,100,97,116,101,0], encoding: .utf8)!

        self.init_example()
    }

    required init?(coder aDecoder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    private func init_example() -> Void {
                aymentTable = answerPurchase(imagesRing:6487.0, btnNormal:String(cString: [114,101,109,117,120,101,114,0], encoding: .utf8)!)
        areaTimers = answerPurchase(imagesRing:9600.0, btnNormal:String(cString: [114,103,98,116,111,121,118,0], encoding: .utf8)!)

    }

    @discardableResult
 func answerPurchase(imagesRing: Double, btnNormal: String!) -> String! {
    var topb: String! = String(cString: [109,117,116,97,116,105,110,103,0], encoding: .utf8)!
    var recognizerj: String! = String(cString: [102,105,108,116,101,114,117,118,0], encoding: .utf8)!
    var phonez: String! = String(cString: [115,116,114,101,97,109,102,111,117,114,99,99,0], encoding: .utf8)!
   repeat {
      recognizerj.append("\(1 / (Swift.max(2, phonez.count)))")
      if 698860 == recognizerj.count {
         break
      }
   } while (!topb.contains(recognizerj)) && (698860 == recognizerj.count)
   for _ in 0 ..< 2 {
      phonez.append("\(recognizerj.count << (Swift.min(labs(1), 2)))")
   }
   repeat {
       var fonto: String! = String(cString: [118,105,115,97,0], encoding: .utf8)!
         fonto.append("\(fonto.count)")
          var indexh: Double = 0.0
         fonto.append("\(fonto.count)")
         indexh -= Double(fonto.count - Int(indexh))
      if fonto.count > 1 {
         fonto.append("\(3 * fonto.count)")
      }
      recognizerj.append("\(3 >> (Swift.min(5, phonez.count)))")
      if recognizerj == (String(cString:[53,117,53,104,99,108,113,111,95,100,0], encoding: .utf8)!) {
         break
      }
   } while (recognizerj == (String(cString:[53,117,53,104,99,108,113,111,95,100,0], encoding: .utf8)!)) && (topb == String(cString:[111,0], encoding: .utf8)!)
      topb = "\(phonez.count)"
   if 4 <= topb.count {
      recognizerj = "\(topb.count)"
   }
   return topb

}


@discardableResult
 func itemResumption(eaderRequest: Int) -> Bool {
    var speechj: String! = String(cString: [114,101,102,100,117,112,101,0], encoding: .utf8)!
    var check3: Double = 0.0
    _ = check3
    var nameu: Bool = true
   withUnsafeMutablePointer(to: &nameu) { pointer in
          _ = pointer.pointee
   }
       var headersS: [String: Any]! = [String(cString: [97,115,110,0], encoding: .utf8)!:String(cString: [115,117,112,112,114,101,115,115,105,111,110,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &headersS) { pointer in
             _ = pointer.pointee
      }
       var phoned: String! = String(cString: [97,115,121,110,99,100,105,115,112,108,97,121,107,105,116,0], encoding: .utf8)!
       _ = phoned
       var markd: [String: Any]! = [String(cString: [97,116,116,114,115,0], encoding: .utf8)!:975, String(cString: [99,111,114,101,115,0], encoding: .utf8)!:481, String(cString: [119,101,98,112,97,103,101,0], encoding: .utf8)!:63]
      for _ in 0 ..< 1 {
         markd = ["\(markd.keys.count)": 1]
      }
      if !phoned.hasPrefix("\(headersS.values.count)") {
          var painterp: Bool = false
          var sheetd: Bool = false
          var atts5: String! = String(cString: [114,97,105,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &atts5) { pointer in
    
         }
          var goodsz: Bool = true
          _ = goodsz
          var paintT: String! = String(cString: [98,112,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &paintT) { pointer in
                _ = pointer.pointee
         }
         phoned = "\(((painterp ? 1 : 3)))"
         sheetd = phoned.count == headersS.values.count
         atts5.append("\(((goodsz ? 3 : 5) % (Swift.max(atts5.count, 1))))")
         paintT.append("\(((painterp ? 2 : 3) * 3))")
      }
      repeat {
         phoned.append("\(headersS.values.count >> (Swift.min(labs(2), 3)))")
         if phoned == (String(cString:[107,107,57,113,102,52,103,0], encoding: .utf8)!) {
            break
         }
      } while (phoned == (String(cString:[107,107,57,113,102,52,103,0], encoding: .utf8)!)) && ((4 + phoned.count) <= 2)
         phoned.append("\(((String(cString:[110,0], encoding: .utf8)!) == phoned ? headersS.values.count : phoned.count))")
      if markd.keys.count > headersS.values.count {
         headersS = ["\(headersS.count)": headersS.keys.count * phoned.count]
      }
       var rateM: [Any]! = [3239]
       var obj3: String! = String(cString: [115,101,116,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
         phoned.append("\(phoned.count * 2)")
      }
      if phoned != obj3 {
         obj3.append("\(phoned.count << (Swift.min(5, rateM.count)))")
      }
      speechj.append("\(((nameu ? 3 : 4) ^ markd.values.count))")
   if 5.17 >= (1.54 - check3) {
      nameu = !speechj.hasPrefix("\(check3)")
   }
      check3 -= (Double(speechj.count | (nameu ? 4 : 5)))
       var picx: Float = 3.0
      repeat {
         picx += Float(3)
         if 1541248.0 == picx {
            break
         }
      } while (1541248.0 == picx) && ((5.42 * picx) < 3.31)
      while ((picx / (Swift.max(4, picx))) >= 2.99 && 1.32 >= (picx - 2.99)) {
         picx -= Float(Int(picx))
         break
      }
         picx -= Float(Int(picx))
      speechj.append("\(Int(picx) | speechj.count)")
       var toolz: Double = 1.0
       _ = toolz
       var sepaks: [Any]! = [226, 560]
         toolz *= Double(3 % (Swift.max(4, Int(toolz))))
       var signA: String! = String(cString: [108,97,117,110,99,104,101,114,0], encoding: .utf8)!
       var replaceZ: String! = String(cString: [97,115,101,108,101,99,116,0], encoding: .utf8)!
      if toolz == 3.57 {
         toolz -= (Double(signA == (String(cString:[105,0], encoding: .utf8)!) ? signA.count : replaceZ.count))
      }
      while (!signA.hasPrefix("\(sepaks.count)")) {
         sepaks = [signA.count]
         break
      }
       var eveant3: Bool = false
       var minimumh: Bool = false
      if minimumh {
         sepaks = [1 - replaceZ.count]
      }
         eveant3 = !replaceZ.hasPrefix("\(eveant3)")
      check3 *= Double(Int(toolz) % 3)
      nameu = check3 > 100.98
   return nameu

}


@discardableResult
 func pointCancel(freeSet: Double, saveUtilsa: Double, sizelabelOllection: Int) -> [String: Any]! {
    var memberG: [String: Any]! = [String(cString: [109,105,120,105,110,103,0], encoding: .utf8)!:String(cString: [99,104,114,111,110,111,0], encoding: .utf8)!, String(cString: [116,101,115,116,0], encoding: .utf8)!:String(cString: [112,97,114,101,110,116,0], encoding: .utf8)!]
    var with_qmc: String! = String(cString: [108,105,109,105,116,97,116,105,111,110,0], encoding: .utf8)!
    var sizelabelL: [String: Any]! = [String(cString: [101,120,101,99,117,116,101,0], encoding: .utf8)!:String(cString: [112,97,105,114,105,110,103,115,0], encoding: .utf8)!, String(cString: [112,111,112,117,112,115,0], encoding: .utf8)!:String(cString: [97,117,116,111,114,101,118,101,114,115,101,115,0], encoding: .utf8)!]
    _ = sizelabelL
       var oneI: Double = 0.0
       var type_ox8: Float = 3.0
      withUnsafeMutablePointer(to: &type_ox8) { pointer in
    
      }
       var str2: String! = String(cString: [118,97,114,105,97,98,108,101,0], encoding: .utf8)!
          var phone9: Float = 2.0
          var zhidinges9: [Any]! = [559, 666, 393]
          var accountlabely: Float = 2.0
         withUnsafeMutablePointer(to: &accountlabely) { pointer in
                _ = pointer.pointee
         }
         oneI *= Double(Int(phone9) / (Swift.max(str2.count, 7)))
         zhidinges9.append(zhidinges9.count + Int(phone9))
         accountlabely /= Swift.max(Float(2), 1)
      for _ in 0 ..< 1 {
         oneI += Double(1 | Int(oneI))
      }
          var agreentk: String! = String(cString: [100,105,115,112,97,116,99,104,0], encoding: .utf8)!
         str2.append("\(Int(oneI) | Int(type_ox8))")
         agreentk = "\(Int(type_ox8))"
         str2.append("\(Int(oneI) / 2)")
         oneI *= Double(Int(type_ox8) / (Swift.max(8, str2.count)))
         type_ox8 -= Float(Int(type_ox8))
         type_ox8 *= Float(1)
      if 3 == (Int(oneI) + str2.count) && 2 == (str2.count >> (Swift.min(labs(3), 2))) {
         oneI -= Double(Int(type_ox8))
      }
         oneI /= Swift.max(2, Double(2 + Int(oneI)))
      with_qmc.append("\(sizelabelL.count * 3)")
      with_qmc = "\(with_qmc.count)"
       var speedsJ: String! = String(cString: [112,111,115,116,105,111,110,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &speedsJ) { pointer in
    
      }
         speedsJ.append("\(speedsJ.count + speedsJ.count)")
      while (speedsJ.count <= speedsJ.count) {
         speedsJ.append("\(1 ^ speedsJ.count)")
         break
      }
      for _ in 0 ..< 1 {
         speedsJ.append("\(speedsJ.count >> (Swift.min(labs(2), 5)))")
      }
      memberG = ["\(memberG.count)": memberG.keys.count]
      memberG = ["\(sizelabelL.values.count)": 2 | sizelabelL.count]
   if sizelabelL.keys.count > 3 {
       var contenH: String! = String(cString: [101,112,115,118,0], encoding: .utf8)!
       var againQ: Double = 2.0
      withUnsafeMutablePointer(to: &againQ) { pointer in
             _ = pointer.pointee
      }
      for _ in 0 ..< 3 {
          var chuang5: String! = String(cString: [115,116,114,100,117,112,0], encoding: .utf8)!
          _ = chuang5
          var candidateu: String! = String(cString: [110,111,116,105,102,105,101,114,0], encoding: .utf8)!
         againQ -= Double(contenH.count)
         chuang5.append("\(Int(againQ))")
         candidateu = "\((contenH == (String(cString:[111,0], encoding: .utf8)!) ? contenH.count : candidateu.count))"
      }
         contenH.append("\(Int(againQ) * 1)")
          var f_heightw: String! = String(cString: [109,106,112,101,103,106,112,101,103,0], encoding: .utf8)!
          _ = f_heightw
          var alabelJ: [Any]! = [305, 422, 642]
         againQ -= Double(contenH.count & 1)
         f_heightw.append("\(f_heightw.count)")
         alabelJ = [f_heightw.count]
         againQ *= Double(contenH.count)
      while ((1 | contenH.count) <= 1 || 1 <= (Int(againQ) / (Swift.max(contenH.count, 8)))) {
         againQ -= Double(Int(againQ))
         break
      }
      if contenH.contains("\(againQ)") {
         contenH.append("\(contenH.count)")
      }
      memberG = ["\(sizelabelL.keys.count)": 2]
   }
   return memberG

}


@discardableResult
 func drawTool(ylabelMessages: [String: Any]!, barColumn: String!) -> [Any]! {
    var namelabelW: String! = String(cString: [102,97,99,116,111,114,115,0], encoding: .utf8)!
    var alamofireN: String! = String(cString: [107,108,97,115,115,0], encoding: .utf8)!
    _ = alamofireN
    var relationJ: [Any]! = [39, 845]
      alamofireN.append("\(relationJ.count * alamofireN.count)")
      relationJ.append(namelabelW.count >> (Swift.min(2, alamofireN.count)))
   repeat {
      relationJ.append(2 >> (Swift.min(4, relationJ.count)))
      if 2892924 == relationJ.count {
         break
      }
   } while (alamofireN.hasPrefix("\(relationJ.count)")) && (2892924 == relationJ.count)
   for _ in 0 ..< 2 {
       var placeholderlabelt: String! = String(cString: [115,105,109,112,108,101,116,97,103,0], encoding: .utf8)!
      if placeholderlabelt.count == placeholderlabelt.count {
         placeholderlabelt = "\(3 | placeholderlabelt.count)"
      }
         placeholderlabelt = "\(placeholderlabelt.count << (Swift.min(labs(1), 2)))"
       var nexta: String! = String(cString: [97,109,111,117,110,116,115,0], encoding: .utf8)!
         nexta.append("\(2 ^ placeholderlabelt.count)")
      alamofireN = "\(((String(cString:[75,0], encoding: .utf8)!) == namelabelW ? relationJ.count : namelabelW.count))"
   }
      alamofireN = "\(alamofireN.count)"
   repeat {
      alamofireN = "\(alamofireN.count ^ 2)"
      if 2046082 == alamofireN.count {
         break
      }
   } while (alamofireN.count < relationJ.count) && (2046082 == alamofireN.count)
   return relationJ

}


@discardableResult
 func contentAnswerResolution(dictionaryList: Double, twoFont: Int) -> String! {
    var bufferM: Float = 4.0
    var settingV: Bool = false
    _ = settingV
    var memberd: String! = String(cString: [116,104,97,110,0], encoding: .utf8)!
   for _ in 0 ..< 1 {
      memberd.append("\(memberd.count >> (Swift.min(labs(3), 3)))")
   }
   for _ in 0 ..< 2 {
      memberd = "\(((settingV ? 5 : 1) / (Swift.max(Int(bufferM), 9))))"
   }
       var yloading7: Bool = true
       var linesl: Bool = true
      repeat {
         yloading7 = yloading7 || linesl
         if yloading7 ? !yloading7 : yloading7 {
            break
         }
      } while (yloading7 ? !yloading7 : yloading7) && (!yloading7)
      while (yloading7) {
         yloading7 = linesl
         break
      }
          var reusablex: String! = String(cString: [119,105,100,116,104,0], encoding: .utf8)!
         yloading7 = linesl && reusablex.count <= 64
         linesl = !yloading7 || linesl
      if !yloading7 {
          var mealh: String! = String(cString: [105,110,115,116,97,108,108,97,116,105,111,110,0], encoding: .utf8)!
          var endX: [Any]! = [197, 568, 7]
          _ = endX
          var compressedK: Int = 1
         withUnsafeMutablePointer(to: &compressedK) { pointer in
                _ = pointer.pointee
         }
          var valueR: [Any]! = [622, 587, 466]
          var goodsF: Float = 1.0
         linesl = 58 < endX.count
         mealh = "\(((linesl ? 3 : 1) << (Swift.min(labs(3), 3))))"
         compressedK *= 1 & compressedK
         valueR.append((1 >> (Swift.min(labs((linesl ? 2 : 2)), 5))))
         goodsF *= Float(compressedK >> (Swift.min(5, labs(2))))
      }
          var detailsG: Double = 3.0
          var ableL: [String: Any]! = [String(cString: [115,99,111,112,101,100,0], encoding: .utf8)!:181, String(cString: [104,97,114,101,0], encoding: .utf8)!:264]
          var actionJ: Float = 0.0
         linesl = 6.4 >= actionJ
         detailsG /= Swift.max(Double(Int(actionJ) / 1), 4)
         ableL["\(actionJ)"] = 1 >> (Swift.min(5, labs(Int(actionJ))))
      settingV = (yloading7 ? !settingV : yloading7)
      settingV = (((settingV ? memberd.count : 79) | memberd.count) < 79)
   return memberd

}


@discardableResult
 func type_nh(namelabelEvent: Float, detectionVoice: [String: Any]!, closeAida: Int) -> [Any]! {
    var beginh: String! = String(cString: [114,101,115,101,97,114,99,104,0], encoding: .utf8)!
    var numberlabel0: [String: Any]! = [String(cString: [101,109,111,116,105,99,111,110,0], encoding: .utf8)!:219, String(cString: [98,101,116,116,101,114,0], encoding: .utf8)!:673, String(cString: [114,101,115,112,111,110,100,115,0], encoding: .utf8)!:221]
    var stylelabel4: [Any]! = [8215]
      numberlabel0["\(stylelabel4.count)"] = 1
   for _ in 0 ..< 1 {
      stylelabel4 = [stylelabel4.count]
   }
      numberlabel0["\(stylelabel4.count)"] = numberlabel0.values.count
   for _ in 0 ..< 2 {
      beginh.append("\(numberlabel0.count)")
   }
      stylelabel4 = [numberlabel0.values.count & beginh.count]
       var styleB: Double = 1.0
       var keywordsK: String! = String(cString: [115,112,105,110,108,111,99,107,0], encoding: .utf8)!
          var ringS: String! = String(cString: [99,121,99,108,101,99,108,111,99,107,0], encoding: .utf8)!
         keywordsK.append("\(Int(styleB) | 3)")
         ringS = "\(keywordsK.count ^ 3)"
      for _ in 0 ..< 1 {
         keywordsK.append("\(((String(cString:[95,0], encoding: .utf8)!) == keywordsK ? Int(styleB) : keywordsK.count))")
      }
         styleB /= Swift.max(Double(keywordsK.count & Int(styleB)), 2)
      if 4 < (Int(styleB) / (Swift.max(keywordsK.count, 6))) {
          var placeholderlabely: Float = 0.0
          var backY: Double = 5.0
          var selectedT: Double = 5.0
          _ = selectedT
          var collect1: Double = 1.0
          _ = collect1
          var water5: String! = String(cString: [118,105,101,119,115,0], encoding: .utf8)!
         styleB *= Double(Int(backY))
         placeholderlabely /= Swift.max(Float(Int(styleB)), 1)
         selectedT -= Double(Int(placeholderlabely) >> (Swift.min(2, labs(2))))
         collect1 /= Swift.max(2, (Double((String(cString:[97,0], encoding: .utf8)!) == keywordsK ? keywordsK.count : Int(placeholderlabely))))
         water5 = "\(Int(backY))"
      }
         keywordsK.append("\(2 - keywordsK.count)")
       var scopy_rlj: [String: Any]! = [String(cString: [101,118,112,111,114,116,0], encoding: .utf8)!:String(cString: [117,115,97,103,101,0], encoding: .utf8)!, String(cString: [105,110,116,101,114,115,112,101,114,115,101,0], encoding: .utf8)!:String(cString: [108,97,109,101,0], encoding: .utf8)!, String(cString: [116,97,108,107,0], encoding: .utf8)!:String(cString: [115,104,97,114,101,0], encoding: .utf8)!]
       var openw: [String: Any]! = [String(cString: [107,101,121,115,0], encoding: .utf8)!:960, String(cString: [112,107,116,99,112,121,0], encoding: .utf8)!:365]
       _ = openw
         scopy_rlj["\(keywordsK)"] = scopy_rlj.keys.count
         openw = ["\(openw.values.count)": (keywordsK == (String(cString:[118,0], encoding: .utf8)!) ? keywordsK.count : openw.keys.count)]
      stylelabel4.append(Int(styleB) & stylelabel4.count)
   return stylelabel4

}


@discardableResult
 func removeTimer(midnightRelation: Float, tapPoint: Bool) -> UIButton! {
    var subringM: Double = 5.0
    var start7: Int = 2
   while ((Double(Int(subringM) + start7)) >= 5.97) {
      subringM += Double(2)
      break
   }
   while (subringM >= 2.53) {
       var theme0: Double = 5.0
          var resolutiont: Double = 1.0
          var utilsaa: String! = String(cString: [102,105,116,122,0], encoding: .utf8)!
         theme0 += Double(3 >> (Swift.min(1, labs(Int(resolutiont)))))
         utilsaa.append("\(Int(theme0))")
      repeat {
         theme0 -= Double(Int(theme0) >> (Swift.min(2, labs(Int(theme0)))))
         if 2337350.0 == theme0 {
            break
         }
      } while (2.71 == (2.48 / (Swift.max(2, theme0)))) && (2337350.0 == theme0)
          var needsv: String! = String(cString: [115,101,113,117,101,110,99,101,114,0], encoding: .utf8)!
          var d_heightG: String! = String(cString: [97,116,116,101,109,112,116,0], encoding: .utf8)!
          _ = d_heightG
         theme0 += Double(1)
         needsv.append("\(Int(theme0) / (Swift.max(3, 10)))")
         d_heightG = "\(Int(theme0) << (Swift.min(needsv.count, 2)))"
      start7 /= Swift.max(Int(subringM) - 1, 5)
      break
   }
   if 3.47 <= (subringM + Double(start7)) || (subringM + Double(start7)) <= 3.47 {
      start7 <<= Swift.min(2, labs(Int(subringM) << (Swift.min(1, labs(start7)))))
   }
     var setBoardy: UIButton! = UIButton(frame:CGRect(x: 258, y: 123, width: 0, height: 0))
     var fromEnd: UIButton! = UIButton()
     var register_wResponse: Double = 9909.0
     var register_nModel: Double = 7228.0
    var spinningBitplane:UIButton! = UIButton()
    setBoardy.frame = CGRect(x: 285, y: 171, width: 0, height: 0)
    setBoardy.alpha = 0.4;
    setBoardy.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    setBoardy.setBackgroundImage(UIImage(named:String(cString: [119,105,110,100,111,119,95,52,0], encoding: .utf8)!), for: .normal)
    setBoardy.titleLabel?.font = UIFont.systemFont(ofSize:10)
    setBoardy.setImage(UIImage(named:String(cString: [99,111,109,112,108,101,116,105,111,110,0], encoding: .utf8)!), for: .normal)
    setBoardy.setTitle("", for: .normal)
    
    var setBoardyFrame = setBoardy.frame
    setBoardyFrame.size = CGSize(width: 78, height: 269)
    setBoardy.frame = setBoardyFrame
    if setBoardy.alpha > 0.0 {
         setBoardy.alpha = 0.0
    }
    if setBoardy.isHidden {
         setBoardy.isHidden = false
    }
    if !setBoardy.isUserInteractionEnabled {
         setBoardy.isUserInteractionEnabled = true
    }

    fromEnd.alpha = 0.0;
    fromEnd.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    fromEnd.frame = CGRect(x: 70, y: 97, width: 0, height: 0)
    fromEnd.setBackgroundImage(UIImage(named:String(cString: [105,109,97,103,101,115,0], encoding: .utf8)!), for: .normal)
    fromEnd.titleLabel?.font = UIFont.systemFont(ofSize:16)
    fromEnd.setImage(UIImage(named:String(cString: [100,114,97,119,0], encoding: .utf8)!), for: .normal)
    fromEnd.setTitle("", for: .normal)
    
    var fromEndFrame = fromEnd.frame
    fromEndFrame.size = CGSize(width: 81, height: 192)
    fromEnd.frame = fromEndFrame
    if fromEnd.alpha > 0.0 {
         fromEnd.alpha = 0.0
    }
    if fromEnd.isHidden {
         fromEnd.isHidden = false
    }
    if !fromEnd.isUserInteractionEnabled {
         fromEnd.isUserInteractionEnabled = true
    }

    spinningBitplane.setBackgroundImage(UIImage(named:String(cString: [99,104,97,116,0], encoding: .utf8)!), for: .normal)
    spinningBitplane.titleLabel?.font = UIFont.systemFont(ofSize:17)
    spinningBitplane.setImage(UIImage(named:String(cString: [97,103,97,105,110,0], encoding: .utf8)!), for: .normal)
    spinningBitplane.setTitle("", for: .normal)
    spinningBitplane.frame = CGRect(x: 235, y: 258, width: 0, height: 0)
    spinningBitplane.alpha = 0.4;
    spinningBitplane.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    var spinningBitplaneFrame = spinningBitplane.frame
    spinningBitplaneFrame.size = CGSize(width: 123, height: 64)
    spinningBitplane.frame = spinningBitplaneFrame
    if spinningBitplane.isHidden {
         spinningBitplane.isHidden = false
    }
    if spinningBitplane.alpha > 0.0 {
         spinningBitplane.alpha = 0.0
    }
    if !spinningBitplane.isUserInteractionEnabled {
         spinningBitplane.isUserInteractionEnabled = true
    }

    return spinningBitplane

}




}